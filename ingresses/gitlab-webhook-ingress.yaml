apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gitlab-webhook-ingress
  namespace: argo
  labels:
    app: demo-coding-agent
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: gitlab.18.208.214.163.nip.io
    http:
      paths:
      - path: /gitlab
        pathType: Prefix
        backend:
          service:
            name: gitlab-eventsource-eventsource-svc
            port:
              number: 12000
